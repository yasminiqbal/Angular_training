//arrays and fetching elements

let values = [];

values = [5,6,7,8];

values.push(5) // it pushes the value to  array works like append

console.log(values);
console.log(values.length);

console.log(values[0])//index value starts from 0

// arrays can have strings, numeric, boolean
// can have multiple data types
// last element cannot be used as -1 like python

let val = [1,3,'yas', {fruit: 'apple'}]
console.log(val)
console.log(typeof(val))
console.log(val[3])

